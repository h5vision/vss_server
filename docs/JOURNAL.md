# JOURNAL — 무엇을 재서 무엇이 나왔고 왜 그렇게 정했나

팀이 읽는 기록이다. 회차마다 **쟀다 → 나왔다 → 판단 → 넘어간 이유 → 아직 아니다** 를 남긴다.
이 서버의 설정값이 왜 그 값인지 궁금할 때 여기서부터 거슬러 올라가면 된다.

규칙: **새 회차는 맨 위에 붙인다. 지난 항목은 고치지 않는다.** 나중에 틀린 것으로 밝혀지면 새 항목에서 정정하고 이유를 적는다.
수치는 여기서 만들지 않는다 — `data/evaluation/runs/*.json` 이 정본이고 여기에는 run_id 와 함께 옮겨 적기만 한다.
개념 설명은 여기 쓰지 않는다. 필요하면 회차별 상세 문서로 링크한다.

---

## 2026-09-13 — 문제지 넷을 자리마다 질문 하나씩으로 다시 썼다. sqlalchemy 원본은 라이브러리 본체를 하나도 안 재고 있었다

**쟀다.** 시험지가 가리키는 정답 자리를 레포의 `ast` 결과와 대조했다. 자리는 (파일, 심볼) 쌍이고, 그 자리가 청커의 코퍼스에 남는지, 인덱서가 그 파일을 수집하는지까지 단계별로 봤다. 도구는 9/12 에 만든 `suite_coverage.py` 와 이번에 만든 `export_suite_md.py` 다.

**나왔다.** 원본 시험지들은 질문 문장이 25~32종류뿐이고 한 문장이 자리 스무 개 안팎을 가리켰다. 같은 질문에 정답이 스무 군데면 검색기가 무엇을 맞혀야 하는지 정의가 안 된다. 표본이 작은 문제가 아니라 채점 불가다.

**sqlalchemy 는 더 나빴다.** 답 120개가 전부 `examples/` 를 가리켜 라이브러리 본체 `lib/sqlalchemy/` 를 하나도 재지 않고 있었다. 경로가 실재해서 검증은 통과했고, 9/12 표에 "gold 상태 정상" 이라 적힌 것은 그 뜻이었다. 무엇을 가리키는지는 그때 안 봤다.
`asyncer` 원본은 `_main.py` 에 없는 심볼 둘(`do_work`, `program`)을 가리켰다. `runnify` 와 `soonify` 의 docstring 안 예제 코드를 자리로 착각한 것이다.
`flask-restplus` 의 답 없는 문항 30개 중 다섯은 그 레포에 실제로 답이 있었다. 토큰 갱신은 `access_token`, 권한 검사는 `permission_required`, 스키마 자동 마이그레이션은 `migrate` 가 하는 일이다.

**판단.** 자리마다 질문을 하나씩 새로 썼다. 소스를 읽고 그 자리가 하는 일로 묻되, 파일 안에서 이름이 겹쳐 자리를 특정 못 하는 것과 청커가 코퍼스에 안 남기는 것은 뺐다. sqlalchemy 는 고치지 않고 라이브러리 본체에서 새로 썼다.

| suite | 문항 (답 + 답없음) | 독립 청크 | 청크 하나 |
|---|---|---:|---:|
| `sqlalchemy-r1` | 463 (433 + 30) | 315 | 0.32%p |
| `flask-restplus-server-example-r1` | 240 (210 + 30) | 182 | 0.55%p |
| `flask-realworld-example-app-r2` | 118 (88 + 30) | 74 | 1.35%p |
| `asyncer-r1` | 98 (68 + 30) | 58 | 1.72%p |

9/9 기준선이 쓴 `fastapi-cli-full` 은 46문항에 독립 청크 7개였다. 청크 하나가 14.29%p 를 움직인다. sqlalchemy 는 0.32%p 로 45배 촘촘하다.

**검증.** 네 suite 모두 id, 질문, 자리 중복이 없고 gold 심볼 799개가 전부 실제 AST 정의다. 평가기의 `load_questions` 는 심볼이 파일 어딘가에 문자열로 있기만 하면 통과시켜서 AST 로 따로 확인했다. 수집 단계도 `collect_files` 를 직접 돌려 gold 파일이 하나도 빠지지 않는 것을 봤다.

**넘어간 이유.** sqlalchemy 범위를 연결 풀, 이벤트, 엔진 열여덟 파일로 한정했다. `lib/` 아래 13,435자리를 다 채울 수 없고, 방언 구현자용 추상 인터페이스(`interfaces.py` 140자리, `default.py` 121자리)는 질문이 겉돌기 쉽다.

**아직 아니다.** run 은 여전히 0회다. 답지 제거 커밋과 재인덱싱이 먼저다. 답 없는 문항 30개가 네 시험지 모두 웹 앱 기능이라 걸러 내기가 쉬워 근거 없음 판정 수치는 후하게 나올 것이다. `api_test` 는 레포 자리가 1,472개인데 시험지가 30문항에 독립 청크 13개로 남아 있고, `vision` 은 청커가 TypeScript 를 줄 단위로만 잘라 심볼 자리가 0이다.

## 2026-09-12 — 브리핑이 예산의 70%를 문서 요약에 쓰고 있었다. 단계에 몫을 줬다

**쟀다.** 9/11 EC2 브리핑 실행 기록(`20260911T012644-7cb6470f`)의 호출별 시간을 단계별로 합쳤다. 그리고 레포 7개에서 문서 묶음이 몇 개 나오는지 실제 코드를 돌려 셌다.

**나왔다.** 문서 요약 6묶음이 319.5초로 600초 예산의 70%를 썼다. plan 35.7초, 주제 조사 65.7초, final 33.7초가 남았고, 주제 8개 중 1개만 조사된 채 7개가 "분석 미완료" 로 발행됐다.

원인은 문서 분량이 아니라 **문서 파일 개수**였다. 한 파일이 묶음 상한의 절반까지만 쓸 수 있어서, 문서 파일이 두세 개인 레포는 저절로 멈추고 일곱 개를 넘으면 상한 6을 채운다. 재 보니 vision 2묶음, fastapi-cli 1, fastapi-new 3, flask-realworld 3, sqlalchemy 6, api_test 6, vss_server 6이다. 묶음 하나는 어느 레포에서나 47~64초다.

**판단.** 전체 예산만 있고 단계 사이에 몫이 없던 것이 문제였다. 먼저 오는 단계가 다 쓰면 뒤 단계는 검사에 걸려 통째로 생략된다. `VSS_BRIEFING_DOC_TIME_RATIO`(기본 0.3)를 두고, 앞서 끝난 문서 호출의 평균으로 다음 호출이 몫을 넘길 것 같으면 시작하지 않게 했다. 첫 묶음은 몫과 무관하게 돈다 — 문서 요약이 하나도 없으면 주제 선정 입력이 비어 브리핑 전체가 실패한다.

**나왔다 (검증).** 같은 레포·같은 예산으로 다시 돌린 `20260912T135317-990d033a` 에서 분석된 주제가 1개에서 5개로, "분석 미완료" 줄이 7개에서 3개로 줄었다. 인용 근거의 마지막 번호가 80에서 98로 늘었고 코드 파일 근거가 4개에서 13개가 됐다. 단계 캐시를 비우고 돌려 캐시적중 0을 확인했다 — 캐시 키에 예산이 안 들어가서 비우지 않으면 옛 응답이 0초에 그대로 나온다.

**넘어간 이유.** 문서 요약 응답의 `conditions` 와 `unknowns` 는 읽는 코드가 없는데 출력의 33%(9/9 실행 기준 671토큰)를 차지한다. 빼면 같은 몫에 문서를 더 읽지만 주제 수는 거의 안 늘어서 뒤로 미뤘다.

**아직 아니다.** 비율 0.3 은 실행 한 번으로 정했고 api_test·sqlalchemy 에서는 안 재 봤다. 주제 조사가 파일 하나에서 2조각까지만 읽어 550줄짜리 `vss/server.py` 의 요청 처리부가 안 읽히는 것, 라우트 추출이 데코레이터 등록만 잡아 표준 라이브러리 HTTP 서버에서 0개가 나오는 것, 브리핑 결과물이 레포에 있으면 다음 브리핑의 근거로 인용되는 것은 손대지 않았다.

## 2026-09-12 — 평가 문항의 자를 다시 셌다. 답지는 네 레포가 아니라 **여섯 레포**에 있었다

**쟀다.** 시험지 8장과 suite 12개의 정답 위치를 AST 와 실제 청커로 대조했다. 문항 수가 아니라 **독립 청크 수**를 셌다. 도구는 이번에 만든 `suite_coverage.py` 다.

**나왔다.** 9/9 기준선의 자가 거칠다. `fastapi-cli-full` 은 답 46문항인데 독립 청크가 7개라 청크 하나가 14.29%p 를 움직인다. `api-test-v1` 은 30문항에 13개다. 그리고 `RAG_TEST` 답지가 들어 있는 레포가 9/11 에 적은 넷이 아니라 여섯이다. asyncer 와 flask-restplus-server-example 도 마지막 커밋이 답지 커밋이었다.

ast 청커가 짧은 메서드를 코퍼스에서 잃는 것도 셌다. flask-realworld 101개 중 8개, sqlalchemy 8,765개 중 295개, fastapi-cli 0개다. line-window 는 0개다.

**판단.** `flask-realworld-example-app` 시험지를 150문항에서 118문항으로 다시 썼다. 자리마다 질문 하나씩 소스를 읽고 새로 썼고, 이름이 겹쳐 자리를 특정할 수 없는 5개와 코퍼스에 안 남는 8개를 뺐다. 수치와 재현 명령은 [docs/EVAL_COVERAGE_20260912.md](EVAL_COVERAGE_20260912.md) 에 있다.

**아직 아니다.** 여섯 레포에서 답지를 지우는 일과 재인덱싱을 못 했다. 그게 안 되면 `fastapi-cli` 말고는 잰 값을 쓸 수 없다. 시험지 여섯 장도 남았고, `cli.md` 와 `vision.md` 는 정답 경로가 각각 다른 레포와 빌드 산출물이라 고치는 게 아니라 새로 만들어야 한다.

## 2026-09-11 — 측정 자를 다시 만들었다. 데모 레포 넷에 **정답지가 코퍼스 안에 있었다**

**쟀다.** 레포별 평가 문항을 늘리려고 시험지와 레포를 대조했다. 두 가지를 셌다. 하나는 시험지가 가리키는 정답 자리 수, 다른 하나는 레포에 실제로 있는 함수·클래스 수(`ast` 로 직접 셈, `tests/` 제외).

**나왔다.** 세 가지가 드러났다.

`fastapi-new` · `flask-realworld-example-app` · `sqlalchemy` · `mockserver-monorepo` 네 레포에 `RAG_TEST.md` 와 `RAG_TEST.json` 이 **git 으로 들어 있다.** 각 61문항의 질문과 정답 위치가 담겨 있고, 세 레포의 마지막 커밋이 바로 그 파일을 넣은 커밋이다. 지금 EC2 의 `*--ast-v3` 인덱스 셋은 그 커밋으로 만들어졌다. 제외했다는 기록은 어디에도 없다.

정답 자리가 레포에 모자란다. `fastapi-cli` 는 `src/`+`scripts/` 에 함수·클래스가 43개, `fastapi-new` 는 22개뿐이다. 그런데 시험지는 답 있는 문항을 120개로 잡아 한 자리를 3~6번 반복해 물었다. 질문 문장도 58~62개 템플릿의 되풀이라 같은 문장이 최대 22번 나왔다.

그래서 n 이 실제보다 커 보였다. 한 자리를 6문항이 가리키면 검색이 그 자리를 놓칠 때 6문항이 통째로 틀린다. 120문항처럼 보이지만 독립 판정은 22번이다.

**판단.** 문항 수를 레포에 맞춰 줄였다. `fastapi-cli` 는 답 있는 42 + 답 없는 30 = 72문항으로 다시 썼다(`evaluation/suites/fastapi-cli-r1.jsonl`, `suite_hash=76f1f9662cedd961`). 한 자리에 한 문항이고 질문은 그 자리에서만 나오는 사실로 썼다. 세 레포는 소스 검증을 마친 개정본 114문항을 `evaluation/` 로 들였다(dev 25 + holdout 13 씩).

정답지는 `--exclude` 로 막지 않고 **레포에서 커밋으로 지운다.** `POST /index` 는 `profile.exclude_globs` 를 안 보내므로 `--exclude` 로는 CLI 로 만든 인덱스만 깨끗해지고 프론트가 만드는 인덱스에는 계속 들어간다. 파일만 지우고 커밋을 안 하면 인덱스 meta 에 `dirty: true` 가 박혀 어느 코퍼스였는지 커밋으로 되짚을 수 없다.

**넘어간 이유.** 문항 수를 채우는 두 길을 버렸다. `tests/` 를 정답으로 쓰면 9/7 에 넣은 경로 감점 규칙 때문에 검색력이 아니라 그 규칙을 재게 된다. 문서와 설정 파일을 쓰면 9/5 에 겪은 라벨 문제가 되살아난다 — 그때 `fastapi-cli` 여지 문항의 절반이 README 가 정답 내용을 갖고 있어 판정을 두 번 틀렸다. 레포가 작다는 사실을 문항 수로 덮지 않기로 했다.

기존 `fastapi-cli-full.jsonl` 61문항은 지우지 않는다. `suite_hash` 가 달라 두 자의 수치는 잇지 않으며, 9/4·9/9 run 이 그 자를 가리킨다.

**아직 아니다.** 새 문제지로 잰 run 이 아직 하나도 없다. 재인덱싱과 측정은 EC2 에서 해야 한다(README 「4-3」). `fastapi-new` 는 레포에 자리가 22개뿐이라 개정본에서도 `new` 한 자리를 5문항이 가리킨다. holdout 은 dev 와 같은 사람이 만들어 독립적인 블라인드가 아니므로 "holdout 을 보고 설정을 조정하지 않는다" 는 규칙으로만 성립한다. `sqlalchemy` 문항 둘은 정답 줄 범위가 파일 길이를 넘어 손봐야 한다.

---

## 2026-09-11 — 커밋별 인덱싱을 저장 계층이 아니라 **인덱스 이름**으로 했다

**쟀다.** 커밋이 왜 안 쌓이는지를 코드에서 확인했다. `revisions` 에 commit 컬럼이 없고 `promote` 가 옛 revision 을 지우는데, 그 범위가 `WHERE project_id = %s` 다. 그리고 자동 선택(`index_candidates`)이 후보를 고르는 기준은 이름이 아니라 **저장된 fingerprint 의 청커 세대**이고, 같으면 `indexed_at` 최신이다.

**나왔다.** 지우는 범위가 이름 단위라면 **이름이 다르면 서로 안 지운다.** 스키마를 안 고치고도 커밋마다 인덱스가 한 벌씩 남는다. 그리고 `--` 뒤 문자열을 읽는 코드는 한 줄도 없다 — `--` 를 쪼개는 두 곳 다 **앞부분**(레포 이름)만 쓴다.

**판단.** `remote`+`branch` 로 들어온 요청의 이름을 서버가 `<레포>@<브랜치>--<청커>-<sha7>` 로 짓는다. `--` 뒤 첫 토큰이 여전히 청커 세대라 기존 규약이 그대로 산다. 프론트가 보내는 값은 안 바뀐다 — `POST /index` 에 `project_id`+`branch`, 조회는 `<레포>` 나 `<레포>@<브랜치>` 다. 대신 자동 선택이 `--` 뿐 아니라 `@` 접두사도 보게 했고, `GET /index/status`·`GET /index/exists` 도 정확한 이름이 없으면 **도는 작업 먼저, 그다음 완성 인덱스** 순으로 찾는다. 순서가 뒤집히면 옛 인덱스를 보고 방금 시작한 작업이 완료로 보인다.

**넘어간 이유.** 브랜치를 안 고른 요청에서 이름의 브랜치를 빼는 안은 버렸다. 브랜치별 구분이 이 작업의 목적인데 그 인덱스만 정체를 잃는다. 대신 clone 뒤 git 에서 실제 브랜치 이름을 읽어 채운다. 빈 값·`"None"`·`"null"`·`"HEAD"` 를 "기본 브랜치" 로 읽으므로, 프론트가 브랜치를 안 고를 때 보내는 문자열 `"None"` 이 `git clone --branch None` 으로 502 를 내던 것도 같이 없어진다.

**아직 아니다.** `--depth 1` clone 이라 **최신 커밋만** 쌓인다 — 과거 커밋을 골라 묻는 것(`search(revision=)`)은 여전히 없다. 이름이 매번 달라 증분 인덱싱이 이 경로에서는 안 걸리고(커밋마다 전체 임베딩 + 브리핑), 같은 이름이 아니므로 `already_running` 이 연달아 누른 인덱싱을 못 막는다. 둘 다 `~/repos/<레포>` 한 폴더를 쓴다. 인덱스·BM25·manifest·브리핑 기록이 커밋 수만큼 쌓이는데 자동 정리는 없다 — `DELETE /projects` 로 손으로 지운다. 그리고 목록에서 짝을 찾을 때는 `index_id` 가 아니라 `name`(레포@브랜치)을 봐야 한다. 커밋이 붙기 전에는 둘이 같은 값이었다.

계약 상세는 [docs/API.md](API.md) 「project_id 이름 규칙」.

---

## 2026-09-10 — 인덱스 삭제를 받는 쪽을 만들었다. 부르는 쪽은 이미 있었다

**쟀다.** 스냅샷 서비스(module)의 클라이언트 코드와 우리 라우트를 코드로 맞대 봤다. 그리고 삭제가 건드리게 될 것을 코드에서 직접 뽑아 셌다 — 인덱스 하나가 남기는 것은 저장소의 벡터와 디스크 사이드카 여덟 종이다.

**나왔다.** module 은 `DELETE /projects?project_id=`(204·본문 없음)를 이미 부르고 있는데 우리 서버에는 `do_DELETE` 가 아예 없었다. 표준 라이브러리가 501 을 돌려주고, module 클라이언트는 500 이상을 "VSS 서버 사용 불가" 로 읽는다. `Store.drop()` 은 양쪽 저장소에 구현돼 있었지만 부르는 곳이 테스트 한 줄뿐인 죽은 코드였다.

**판단.** 라우트를 만들고 삭제 범위를 "벡터 + 그 인덱스가 만든 사이드카" 로 정했다. 지우지 않는 것 셋은 소스 디렉터리(`~/repos/<레포>` 하나를 여러 인덱스가 같이 쓴다 — 실측으로 `vision` 5개, `fastapi-cli` 5개), chroma 의 전역 sqlite, 전역 인덱싱 이력 로그다. 지우는 순서는 브리핑 먼저다 — 고아가 됐을 때 거짓말하는 파일이 브리핑 하나뿐이라(`GET /briefing` 은 저장소를 안 본다) 중간에 죽어도 거짓말이 안 남는다. 이름은 자동 선택을 태우지 않는다. 태우면 형제 인덱스를 지운다.

**넘어간 이유.** 없는 이름에 404 대신 204 를 준다. module 이 404 를 "이 배포본에 삭제 라우트가 없다" 로 번역하기 때문이다. 질의 로그는 `index_id` 로 지운다 — `project_id` 컬럼은 프론트가 보낸 레포 이름이라 그것으로 지우면 같은 레포의 멀쩡한 인덱스 행까지 사라진다. 인덱싱 중 삭제는 막지 않는다. 그 인덱싱이 실패로 끝나는 쪽을 택했다.

**아직 아니다.** EC2 반영과 pgvector 검증. `Store.drop()` 이 서비스 경로에서 처음 불리는데 pgvector 쪽은 한 번도 안 돌아봤다. 커밋별 인덱스 이력(`keep_revisions` 는 지금 1 로 고정이라 직전 한 벌만 남는다)은 착수 전이고, 그것을 켜면 module 의 `completed_for()` 가 과거 커밋 스냅샷을 전부 실패로 떨어뜨리므로 상대 쪽이 먼저 바뀌어야 한다. 질의 로그에서 인덱스가 정해지기 전에 실패한 질의(모델 미로드·Ollama 연결 실패)는 `index_id` 가 비어 있어 안 지워진다 — 질문 본문이 남는다.

계약 상세는 [docs/API.md](API.md) 「삭제」.

---

## 2026-09-09 — 브리핑을 실제 모델로 세 번 돌려 고쳤다. 같은 10분 안에 조사가 두 배로 들어간다

**쟀다.** fastapi-cli 인덱스(`fastapi-cli--ast-v3`)로 브리핑 3회 — run `20260909T083250-505c712c`, `20260909T123207-a85caaac`, `20260909T135149-fbcca9dc`. 호출마다 어림 토큰·실제 토큰(`prompt_eval_count`)·글자 수·시간·종료 사유를 기록했다. 수치 원본은 EC2 실행 기록이고 3회차 사본은 `docs/briefing_tuning_20260909/`.

**나왔다.** 1회차는 535초에 주제 8개 중 4개만 조사했고 문서 단계가 212초(40%)였다. 토큰 어림(영문 2자·한글 1자 = 각 1·2토큰)은 실제의 약 2배였다(비율 평균 0.48). 3회차는 530초에 주제 8개 전부와 보완 라운드까지 들어갔고 문서 단계 86초, 출력 잘림 0회. 실측 계수는 코드 영문 3.3~3.5자/토큰, 마크다운 2.6, 한글 0.46~0.59토큰/자.

**판단.** 계수는 마크다운에 맞춘 2.8/0.6 으로 두고 설정으로 뺐다. 시간 예산 600초, 주제 순서는 대표 → 모델 선택 → 고정, 변경 이력 문서는 맨 뒤·묶음 1개, 문서 호출은 배열 3개 형식으로 짧게, 테스트 파일 라우트는 한 줄로 접는다. 진입점별 함수 헤더는 본문에 되살렸다(CHARTER 범위 3). 근거 검증은 응답 단위가 아니라 문장 단위로 하고, 생성 중 소스가 바뀌면 버리지 않고 표시해 낸다. Mermaid 는 뺐다. 상세는 [BRIEFING_TUNING_20260909.md](BRIEFING_TUNING_20260909.md).

**넘어간 이유.** 실제 모델에서 확인된 문제만 고쳤고 레포 이름·경로를 보는 규칙은 넣지 않았다. 2회차에서 출력 상한만 줄였다가 README 묶음을 잃은 것은 형식을 줄여서 풀었다.

**아직 아니다.** 한글 비율이 다른 레포(api_test)로 계수 확인, 마지막 수정(⑫) 뒤 실행 1회, 증분 인덱싱의 pgvector 테스트, `/index` 훅의 동기 실행과 공유 clone 폴더는 스냅샷 백엔드 조정 뒤.

---

## 2026-09-08 — 검색 개선을 멈추고 스냅샷(브랜치·commit) 연동으로 방향을 바꿨다

**쟀다.** 새 run 은 없다. 9/4 fastapi-cli run(`20260904T005910Z-5e6307`, ast-v2 hybrid retrieval)의 저장된 top-5 를 한 번 더 세었다 — top-3 자리 138개 중 `.github/workflows/*.yml`·`release-notes.md` 가 27개(테스트 파일 56개는 9/7 에 이미 뒤로 보냈다), gold 가 그 파일인 문항은 0. 파일 단위 실패 13문항 중 5문항은 상위 3 이 전부 이 계열이다. 저장된 top-5 만으로 감점을 흉내내면 36 → 37/46(하한 — pool 6~20위가 저장돼 있지 않다).

**나왔다.** 스냅샷 백엔드(`test-merge` 의 `module/`)는 이미 이 서버의 `POST /index` 계약에 그대로 붙어 있다. 브랜치마다 `~/repos/.snapshot-worktrees/<repo>/<uuid>/branches/<branch>` 에 working copy 를 만들어 `project_root` 로 넘기고, 완료는 `GET /index/status` 의 `index.commit` 으로 판정한다. 이 서버 쪽에 없는 것은 셋이다 — 브랜치 기록(commit sha 만 남는다), 증분(매번 전체 청킹·임베딩), 이력(승격하면 직전 revision 은 보관 1개 뒤 삭제되고 API 로는 active 만 보인다).

**판단.** 검색 개선(`.github` 감점)은 시간 문제로 여기서 멈춘다. 남은 시간은 브랜치·commit 연동에 쓴다. 순서는 `/index` 에 `branch` 를 받아 meta 로 내기 → commit 별 증분(안 바뀐 파일의 청크를 직전 revision 에서 복사, 바뀐 파일만 임베딩) → 이력(옛 revision 보존 + revision 을 고르는 검색). 인덱스 이름은 `<repo>-<branch>` 로 두고 `--` 뒤는 청커 세대로만 쓴다.

**넘어간 이유.** 증분과 이력은 revision 행이 이미 있는 저장 계층 위에 얹는 일이라 빌드→승격과 불변 조건이 그대로다. 브랜치별 소스 트리는 스냅샷 백엔드가 이미 만들고 있어 이 서버가 clone 을 다시 구현할 이유가 없다.

**아직 아니다.** 증분은 CHARTER 가 "하지 않는 것" 으로 적어 둔 항목이라 결정 기록이 먼저다. 이력을 켜려면 Extension 이 branch·commit 을 보내야 한다. `.github` 감점의 진짜 효과는 pool 전체가 저장된 run 이 있어야 잰다.

---

## 2026-09-07 (2) — 브리핑을 "근거 검증형 파이프라인" 으로 교체했다. 실제 모델 검증은 아직이다

**쟀다.** 새 run 은 없다. 비교 실험 없이 설계 문서 검토로 확정했고, 구현은 가짜 모델로 생성 순서·근거 번호 검사·입력 예산·실패 보존을 테스트했다(전체 124개, 브리핑 18개).

**나왔다.**

- 옛 브리핑은 LLM 호출 2~8회(개요 1 + 문서별 1)에 근거 번호를 모델이 직접 썼다. 새 파이프라인은 조사 → 문서 분석 → 주제 선정 → 주제별 근거 수집·분석 → 보완 → 최종 개요 순이고, 모델 출력은 JSON 이며 근거 id 가 제공한 것과 다르면 거부한다. 번호는 코드가 붙인다. 호출 상한은 실행당 40회.
- 검색 인덱스는 소스가 git 체크아웃이고 HEAD 가 인덱스 commit 과 같고 dirty 가 아닐 때만 쓰고, 그때도 "어느 파일 어느 줄을 읽을지" 후보로만 쓴다. 근거 본문은 로컬 원문이다.
- 진입점별 함수 목록·라우트 표·Mermaid 는 본문에서 뺐다. 대신 기능·주제별 상세 설명과 "확인이 필요한 사항" 절이 생겼다.

**판단.** 근거 없는 문장을 구조로 막는 쪽을 택했다. 최종 개요를 마지막에 두고, 실패하면 이전 브리핑을 보존한다. 같은 날 두 가지를 더 고쳤다 — 최종 종합 입력이 예산을 넘으면 실패 전에 잃는 것이 적은 순서로 줄이고(`quality_status=partial` 로 표시), 생성 호출 timeout 은 chat 의 4배로 두되 주제 단계의 timeout 은 그 주제만 실패로 둔다.

**넘어간 이유.** 사전 비교 실험은 설계 확정의 조건으로 두지 않았다. 지금 필요한 것은 "근거 없는 문장이 나오지 않는 브리핑" 이고, 옛 형식과의 정확도 비교는 잴 자(gold)가 없다.

**아직 아니다.** 실제 27B 모델에서의 품질·생성 시간·VRAM. 토큰 추정이 보수적이라(한글 1자 = 2토큰) 실측 `prompt_eval_count` 로 계수를 정해야 한다. 생성 중 `/v1/chat` 지연, `/index` 훅의 동기 생성, 응답 `routes`·`mermaid`·`structure` 모양 변경(Extension 쪽 확인 필요), 실행 기록 디렉터리 정리는 남은 문제다. CHARTER 범위 3 의 "진입점별 함수 헤더" 를 되살릴지는 미정.

---

## 2026-09-07 — 다음 세대는 `ast-v3` + 휴리스틱 재정렬. cross-encoder 리랭커는 넣지 않는다

**쟀다.** 새 run 은 없다. 9/4 run 두 건(`20260904T005826Z-2f0879` api-test, `20260904T005910Z-5e6307` fastapi-cli)의 저장된 `top_paths`(상위 5개)를 다시 세어 "정답이 못 올라온 이유" 를 문항 단위로 열어 봤다.

**나왔다.**

- top-5 에 같은 파일이 두 번 이상 든 문항이 api-test 28/30, fastapi-cli 35/46. AST 청크가 같은 헤더(경로·스코프)를 달아 벡터가 뭉친다. 같은 파일을 앞자리 하나로 접으면 path-level Hit@3 가 api-test 80.0 → 93.3%, fastapi-cli 71.7 → 78.3% (top-5 안 계산이라 하한).
- fastapi-cli 는 top-3 자리 138개 중 56개가 테스트 파일인데 gold 가 테스트인 문항은 0. 테스트를 뒤로 보내면 +3문항(하한).
- 리랭커가 고칠 수 있는 "pool 안, top-3 밖" 문항은 api-test 6, fastapi-cli 8. api-test 의 4문항은 README:244·legacy_app.py 가 정답 문장을 그대로 가져 관련도로 매기면 그쪽이 더 올라간다(라벨 문제). 남는 것은 2문항 = 6.7%p, n=30 의 신뢰구간(±14%p) 안이다. fastapi-cli 의 5문항은 경쟁자가 6~31위라 저장돼 있지 않아 판단 불가.
- 못 맞춘 14문항 중 질문에 gold 심볼과 맞는 이름이 있는 문항은 0. 이름이 있는 문항은 이미 거의 맞는다(api-test 11/13, fastapi-cli 18/19).

**판단.** 병목은 "관련도를 잘못 잰다" 가 아니라 "같은 파일이 슬롯을 먹고 테스트가 원본 위에 온다" 다. 그래서 cross-encoder 리랭커(새 모델 상주, GPU 여유 2.5 GiB)와 심볼 인식(낱말 일치 재정렬은 Hit@1 을 깎는 경향)은 넣지 않고, 휴리스틱 재정렬 둘(파일당 상한 2, `tests/` 경로 뒤로)을 질의 시점 토글로 넣었다. 순서만 바꾸고 `top_score` 는 그대로다.
청커는 `ast-v3` 로 올렸다. 노드 추출은 v2 와 같고, BOM 파일(api_test `.py` 19/300)이 줄 윈도우로 떨어지던 것을 v3 분기 안에서만 고쳐 v1·v2 지문의 코퍼스는 그대로 재현된다. 재정렬은 인덱스가 v3 이상일 때만 자동으로 켜져, 한 run 의 v2 셀은 9/4 와 같은 조건이고 v3 셀만 청커 + 재정렬이다.

**넘어간 이유.** 재정렬 on/off 를 따로 재는 셀은 두지 않았다. 지금 필요한 것은 원인 분해가 아니라 "이전 대비 올랐는가" 다. 라우트 색인, 인덱스별 threshold 자동 보정, 청크 설명 임베딩 같은 다음 후보는 레포와 gold 를 늘린 뒤 판단한다.

**아직 아니다.** EC2 재인덱싱(`--ast-v3` 2개)과 두 matrix 재측정. 재정렬의 손해 쪽(테스트 코드를 묻는 질문, 한 파일의 여러 청크가 답인 질문)은 두 suite 에 문항이 없어 못 잰다. fastapi-cli 의 threshold 병목 9문항은 이번 변경 밖이다. 재집계 방법은 [docs/ACCURACY.md](ACCURACY.md) 7절과 같다.

---

## 2026-09-06 — 서버가 모델을 올리는 길을 닫았다. 모델 상태는 기동 때만 바꾼다

**쟀다.** 9/5 에 잡은 원인("브리핑 훅의 모델 이름이 로드 요청이 되어 상주 모델을 내린다")을 코드에서 세어 보니, 모델 이름을 고르는 자리가 `llm.resolve_model` 하나였고 그 함수는 Ollama 를 보지 않고 `.env` 값이나 요청값을 그대로 돌려줬다.
고친 뒤 가짜 Ollama(모든 `/api/chat` 의 모델 이름과 그 순간 올라온 목록을 기록)에 실제 서버를 붙여 HTTP 19경우, 기동 5경우를 돌렸다. EC2 는 `nvidia-smi` 와 `ollama ps` 를 봤다.

**나왔다.**

- 안 올라온 모델을 `/index`·`/briefing`·`/v1/chat` 에 넣어도 `/api/chat` 로 그 이름이 나가는 일이 0건. 응답은 `503 model_not_loaded`(스트리밍은 `event: error`), 인덱스는 done 그대로.
- unit test 81/81 이 통과한 뒤에도 통합에서 결함 하나가 더 나왔다 — `VSS_ALLOW_MODEL_OVERRIDE=false` 면 고른 모델을 다시 해석해 `.env` 모델로 바꿔 보냈다. 같은 회차에 수정.
- EC2 실측: `qwen3.8:27b` 18,270 MiB + `bge-m3` 850 MiB, 둘 다 100% GPU · Forever. 24 GB 에서 여유 약 2.5 GiB.

**판단.** 요청 경로는 올라온 모델 중에서만 고른다(`pick_model`). 모델 상태를 바꾸는 자리는 기동 한 곳(`_prepare_models`)으로 두고, 거기서도 없는 목표 모델만 올리고 남의 모델은 내리지 않는다. `keep_alive=-1` 은 모든 요청에 싣는다 — Ollama 는 마지막 요청의 값으로 만료를 다시 잡는다.
생성 모델은 qwen2.5-coder 를 폐기하고 `qwen3.8:27b`(대안 `gpt-oss:20b`)로 간다.

**넘어간 이유.** "기동 때 남의 모델을 전부 내리기" 는 공용 노드에서 서버 재시작이 운영 판단을 대신하는 것이라 하지 않았다. 두 모델이 올라간 채 2.5 GiB 가 남아 내릴 이유도 없다.

**아직 아니다.** EC2 반영(pull·재시작)은 커밋 `d06844f` 이후다. "없으면 올린다" 경로는 실제 Ollama 에서는 아직 돌지 않았다(두 모델이 이미 떠 있어 "이미 올라옴" 경로만 돈다). 기동 경로는 unittest 가 없고 세션 안의 가짜 Ollama 로만 확인했다. 27B + bge-m3 상주는 여유가 2.5 GiB 라 세 번째 모델이 올라오면 Ollama 가 둘 중 하나를 내린다 — 코드가 아니라 노드 운영 규칙이다.

---

## 2026-09-05 — 첫 세대 비교가 나왔다. 그리고 자에 결함 넷이 섞여 있었다

**쟀다.** 9/4 새벽 `--ast-v2` 로 재인덱싱한 두 레포를 새 suite 로 쟀다. run `20260904T005826Z-2f0879`(api-test, 답 30) · `20260904T005910Z-5e6307`(fastapi-cli, 답 46).
셀은 줄 윈도우 / ast-v1 / ast-v2 × vector / hybrid, 한 표에 나란히.

**나왔다.**

- api-test 는 **ast-v2 hybrid 가 Hit@3 80.0%, MRR 0.716** 으로 ast-v1 대비 +16.7%p. 노이즈선(3.3%p)의 5배다. 답 30개 전부가 top-20 안에 있다(pool_recall 100%).
- fastapi-cli 는 같은 변경이 +2.2%p(1문항)라 판정이 안 된다. 코퍼스가 306 → 315청크로 거의 안 바뀌었다.
- 두 레포 차이 19.1%p 의 절반 이상이 **측정 결함**이었다. gold 표기가 api-test 는 symbol, fastapi-cli 는 2~8줄 line range 라 같은 파일을 찾아도 오답으로 센다. 파일 단위로 맞추면 fastapi-cli 71.7%, 차이 8.3%p, 신뢰구간이 겹친다.
- matrix `top_k` 가 4 인데 서빙은 8 — 서비스에서는 근거에 들어가는 문항 6개를 eval 이 실패로 센다.
- api_test `.py` 19개가 BOM 이라 AST 청킹을 못 타고 줄 윈도우로 조용히 떨어졌다. gold 3건(API-014·028·029)은 정답을 파일 하나로만 인정한다.
- 임계값은 레포마다 반대다. api-test 는 0.56 이 손실 0(hard negative 6→9/10), fastapi-cli 는 0.54 가 이미 최적. 전역값 하나로 둘 다 못 맞춘다.

**판단.** ast-v2 는 api_test 에서 확정. 리랭킹은 "pool_recall 100% → +20%p 여지" 가 상한을 기대값으로 읽은 것이라 **철회** — 여지 6문항을 열어 보니 4문항은 README 나 legacy_app.py 가 답을 갖고 있어 relevance 리랭커는 그쪽을 굳힌다.
대신 같은 파일 중복만 지워도 24→28/30 이 돌아온다(저장된 top-5 재집계). Parent-Child 는 값어치 없음이 아니라 판정할 자(생성 품질)가 없어 defer. 데이터셋 확대는 맞지만 자 결함 넷을 먼저 걷는 것이 순서다.

**넘어간 이유.** 8/27 과의 재현성(fastapi-cli pipeline 셀이 코드 변경으로 Hit@1 37.0→41.3)은 발표가 끝나 목적이 사라져 파지 않는다.

**아직 아니다.** 위 보정값은 저장된 run 을 다시 센 것이라 원본이 아니다. `metrics` 에 path-level 지표를 넣고 matrix `top_k` 를 8 로 고쳐 다시 run 을 돌려야 `data/evaluation` 에 남는다.
생성 품질은 여전히 자동으로 안 나온다. 상세와 계산법은 [docs/ACCURACY.md](ACCURACY.md).

---

## 2026-09-04 — 자를 바꿨다: `api_test` 질문 40문항, 세대 비교를 한 표로

**쟀다.** 재기 전에 자부터 골랐다. `api_test` gold 후보가 셋이었다 — P 가 준 40문항, 92문항짜리 Blind Test + Ground Truth, 그리고 8/27 부터 쓰던 8문항 smoke.
셋을 `evaluation/README.md` 의 계약(`id`·`question`·`answerable`·`type`·`gold`·`tags`)에 대고 기계로 셌다.

**나왔다.**

- 40문항 쪽은 `path`+`symbol` 쌍이 **36개 전부** 붙어 있었다. 92문항 쪽은 Evidence 87줄 중 심볼이 **10줄(11%)** 뿐이고, 나머지는 경로만 있어 사람이 심볼을 붙여야 한다.
- 92문항 쪽은 참조 경로 13종 중 셋이 `backend/` 밖이고 `docs/2026-08-17_현재_진행_사항.md` 를 3번 가리킨다. 그 문서가 동결 코퍼스에 없으면 답 있는 문항이 조용히 실패로 잡힌다.
- 40문항의 hard negative 10건은 전부 "이 코퍼스에 없는 프레임워크"(Kafka·Django·Flask·gRPC…)라 `has_evidence=false` 화면 검증에 그대로 쓴다.

**판단.** 40문항으로 간다. 변환한 `evaluation/suites/api-test-v1.jsonl` 을 동결 커밋 `2dea3d71` 의 실제 레포에 대고 검증해 경로 13종·심볼 36개가 전부 실물로 존재함을 확인했고,
코퍼스 제외 규칙(`tests,admin/**,.snapshot-admin-backup/**`)에 걸리는 gold 경로는 0건이었다. 답 있는 문항이 6 → 30 이 되어 **문항 하나의 무게가 16.7%p 에서 3.3%p 로** 떨어진다.
함께 `api-test`·`fastapi-cli` 두 matrix 에 `--ast-v2` 셀을 넣어, 한 번의 run 이 **줄 윈도우 / ast-v1 / ast-v2 × vector / hybrid** 를 나란히 내게 했다.

**넘어간 이유.** 9/1 항목이 "재인덱싱 전에는 어떤 개선 수치도 말할 수 없다" 로 끝났는데, 그 뒤로도 잴 자가 없어 코드만 쌓였다. 자를 먼저 세우지 않으면 재인덱싱을 해도 판정이 안 된다.
92문항 쪽은 폐기가 아니라 2차다. 심볼 87줄을 붙이는 사람 작업이 앞에 있어 9/9 동결 전에 들어올지 불확실하다.

**아직 아니다.** 아무것도 측정하지 않았다. `suite_hash` 가 `99bd500d96ffb655` 로 바뀌어 **8/27 run 과 직접 비교되지 않는다** — 새 시리즈의 첫 기준선이 된다.
생성 품질(Answer Correctness·Faithfulness·Hallucination)은 여전히 자동으로 안 나온다. `vss.eval run` 은 LLM 을 부르지 않고 검색까지만 잰다.
`rag_lab` 은 EC2 에 코퍼스가 없어 이 표에 못 올라간다 — 데모 시나리오 S3·S4 가 계속 묶여 있다.

---

## 2026-09-01 — `ast-v1` 은 "다른 청킹 방식"이 아니라 결함 버전이었다 (8/30 항목 정정)

**쟀다.** 8/30 항목은 `ast-v1` 과 `ast-v2` 를 "청커가 다른 실험 셀" 로 적었다. 그 표현이 맞는지 두 청커에 같은 소스를 넣어 직접 돌렸다.

**나왔다.** 두 가지가 나왔다.

- 제어문 아래 정의를 통째로 놓친다. `try/except` 로 import 를 감싸고 `if` 아래 함수를 두 벌 정의한 파일에서 `ast-v1` 이 찾은 심볼은 클래스 하나뿐이었고, 함수 둘은 아예 안 잡혔다.
- 청크가 크기로 갈릴 때 **맥락 헤더가 다른 함수 이름을 단다.** 함수 두 개짜리 파일에서 `ast-v1` 이 낸 두 번째 청크는 헤더가 `# x.py > def parse_fallback` 인데 담긴 코드는 대부분 그다음 함수였고, 시작 위치도 줄 중간이었다.

**판단.** 둘째가 검색에 직접 걸린다. 맥락 헤더는 청크 본문에 붙어 그대로 임베딩되므로(`VSS_CONTEXT_HEADER=on`, 8/27 [제안 E]), 틀린 이름이 벡터에 섞여 있다. 이건 두 설계의 차이가 아니라 한쪽의 결함이다.
따라서 **8/27 `--ast` run 2건은 "개선 전" 기준선으로 인용하지 않는다.** 대신 `--lines` 인덱스가 기준선을 맡는다 — 줄 윈도우 경로는 `python_nodes` 를 거치지 않아(`vss/chunker.py`) 이 결함과 무관하고, 8/27 수치가 그대로 유효하다.

**넘어간 이유.** 지금 서비스 중인 인덱스 4개가 모두 `ast-v1` 이라, 어떤 개선 수치도 재인덱싱 전에는 말할 수 없다. 청커 코드는 8/31 커밋(`9a97d87`)으로 이미 고쳐져 있고 남은 것은 EC2 재인덱싱 하나다.
`ast-v1` 구현 자체는 지우지 않고 동결해 둔다. 8/27 run 이 저장한 fingerprint 가 가리키는 코퍼스를 재현할 수 있어야 "왜 그 수치를 안 쓰는가" 를 보일 수 있다.

**아직 아니다.** `ast-v2` 전체 재인덱싱과 같은 suite 재측정. 그 전까지 비교 대상은 `--lines` ↔ `--ast(ast-v2)` 이고, `ast-v1` 은 어느 쪽 표에도 올리지 않는다.

---

## 2026-08-30 — 같은 fingerprint의 코퍼스를 바꾸지 않고 AST 누락을 `ast-v2`로 분리했다

측정 회차가 아니다. Python AST 추출의 재현 결함을 고치고, 과거 기준선의 의미를 보존하는 호환 경계를 코드와 테스트로 고정한 회차다.

**쟀다**

- `python_nodes`에 제어문 아래 함수, 팩토리 내부 핸들러, 중첩 클래스가 들어오는지 최소 fixture로 직접 비교
- Claude Code가 바꾼 라우트 AST 추출에서 set·동적 `methods=`와 비라우터 데코레이터가 정확히 처리되는지 집중 회귀 테스트

**나왔다**

- `ast-v1`은 fixture에서 최상위 `factory`와 `Outer`만 반환했다. `if`·`try/except` 아래 함수와 `factory.handler`, `Outer.Config` 아래 심볼은 독립 청크가 아니었다.
- 새 라우트 추출은 `methods={"POST", "PUT"}`을 기본 GET으로 오보고하고, `methods=["POST", METHOD]`에서는 동적 요소를 버렸다. `api_route`의 methods 생략도 실제 기본 GET 대신 ANY로 기록했다.
- 메서드명과 `/` 경로만 확인하면 `@pook.get("/users")`·`@pytest.mark.get("/integration")`도 라우트로 오인했고, mock 객체를 `api` 변수에 담으면 이름 휴리스틱을 우회했다.

**판단**

- 기존 구현은 `_python_nodes_v1`로 동결하고, lexical scope를 유지하며 재귀 순회하는 `_python_nodes_v2`를 별도로 추가했다. 기본 청커는 `ast-v2`지만 명시적 `ast-v1`은 이전 코퍼스를 그대로 만든다. 클래스 선언은 독립 청크로 남기고, 부모 함수에서는 중첩 정의 본문을 줄 수 보존 placeholder로 바꿔 이중 가중을 막는다.
- `chunker` 값 자체가 fingerprint에 있으므로 `CORPUS_RULES`는 올리지 않았다. `ast-v1`과 `ast-v2` 결과를 같은 조건으로 취급하지 않는다.
- 라우트 메서드는 set을 포함한 정적 컨테이너를 처리하고, 동적 식을 부분 손실하지 않으며 `api_route` 생략값을 GET으로 기록한다. 라우트 객체는 프레임워크 생성자 대입과 app/router 계열 이름으로 제한하되 명시적인 test/mock 생성자 대입은 제외한다. 브리핑은 중첩 symbol의 전체 qualified name을 표시한다.

**넘어간 이유**

검색 코퍼스를 바꾸는 수정이므로 버전 경계를 먼저 세워야 했다. 이전 평가를 소급해 다른 코퍼스의 결과로 만드는 것보다 새 fingerprint로 전체 재인덱싱하는 비용이 작고 추적 가능하다.

**아직 아니다**

- EC2의 기존 인덱스는 모두 `ast-v1`이다. `ast-v2` 전체 재인덱싱과 동일 suite 재측정은 md가 EC2에서 실행해야 한다.
- 이 회차는 검색 품질 수치를 만들지 않았다. 개선 여부는 저장된 `ast-v2` fingerprint와 고정 코퍼스 revision이 들어간 새 run으로만 판정한다.

---

## 2026-08-28 — 프론트 연동을 실제로 통과시키고, 질의 흐름 결함 7건을 닫았다

측정 회차가 아니다. **"코드는 맞는데 답이 안 나온다" 는 보고 두 건을 끝까지 추적한 회차**다.

**쟀다**

- Extension 이 `project_id` 에 레포 이름(`api_test`)을 보내면 어느 인덱스로 가는가 — EC2 `/search` 왕복 + 노트북에서 입력 10종의 별칭 해석표
- 외부 코드 리뷰가 지적한 질의 흐름 결함 7건이 실제로 재현되는가 — 재현 스크립트 실행 + 각 지점 코드 대조

**나왔다**

- **`project_id` 별칭은 코드가 아니라 설정 문제였다.** 별칭 코드는 EC2 가 서 있던 커밋에 이미 있었고, `.env` 에 `VSS_PROJECT_ALIASES` 한 줄이 없었다.
  넣은 뒤 `{"project_id": "api_test", "index_id": "api-test--ast"}` 확인. `api_test`·`api-test`·`API_TEST` 가 모두 같은 곳으로 가고, 없는 이름은 조용히 넘어가지 않고 `project_not_found` 다.
- **결함 7건 전부 사실이었다.** 그중 가장 심한 것: 모델이 없는 번호(`[7]`)를 인용하거나 답변에 `items[0]` 같은 코드 표기만 있으면
  **출처 목록이 통째로 빈 배열이 됐다.** 본문에는 각주 번호가 보이는데 출처가 없는 화면이 된다.
- 리뷰에 없던 것도 나왔다: 최종 응답의 `timing` 이 검색 구간만 실어 **`prompt_ms` 가 애초에 응답에 오지 않았고**, 계약 문서는 온다고 적혀 있었다.

**판단**

- 출처 전멸(D·E)은 **코드로 고쳤다** — 인용 번호를 `contexts` 범위로 거른다. 범위 밖만 인용하면 "인용 0건" 과 같게 취급해 전부 유지한다. `[N]` 번호는 재부여하지 않는다(불변 조건 4).
- `prompt_ms` 는 **이름과 구간을 맞췄다** — `prompt_ms` 는 프롬프트 조립만, `pre_llm_ms`(신설)가 요청~LLM직전 누적이다. 발표에 타이밍을 쓸 계획이 있어 지금 바로잡았다.
- 나머지 셋(`has_evidence` 의 뜻 · 스트리밍의 HTTP 상태 · `rag:false` 의 meta 모양)은 **동작을 바꾸지 않고 계약에 명시**했다. 9/9 동결이 가깝고, 셋 다 "현재 동작이 무엇인지 몰라서" 생기는 문제였다.

**넘어간 이유**

계약을 K·Y 에게 넘기는 날이라, 프론트가 잘못 분기할 수 있는 지점을 먼저 닫아야 했다. 특히 `metadata.has_evidence`(검색 단계)와 `no_evidence`(최종 결과)는
**한 응답에서 둘 다 true 가 정상적으로 나온다** — "근거는 찾았지만 그것으로 답할 수 없었다" 는 뜻이다. 여기서 잘못 분기하면 "근거 없음" 화면이 뜨지 않는다. `docs/API.md` 에 표로 넣었다.

**아직 아니다**

- **답이 나와야 할 질문에 "근거 없음"이 나온다.** api_test 에 실제로 있는 주제(`worker`)를 물어도 `NO_EVIDENCE` 였다. 검색이 막힌 건지 모델이 거절한 건지 아직 가르지 못했다.
  관련 신호: 코퍼스와 무관한 질문조차 `top_score 0.5996` 으로 임계값 0.54 를 넘는다 — 이 코퍼스에서 임계값이 거의 걸러 내지 못한다는 뜻이다. 9/4 고정 질문 선정 전에 풀어야 한다.
- `api_test` gold 는 아직 8문항이라 이 코퍼스의 정확도를 판정할 수 없다(1문항 = 16.7%p). 40문항이 임계 경로다.
- 오늘 수정은 아직 EC2 에 반영되지 않았다 — 도는 서버는 이전 커밋이다.

---

## 2026-08-27 — EC2 첫 가동 · pgvector 전환 · 기준선 측정

서버를 EC2 에 처음 올리고(PostgreSQL 14 + pgvector 0.8.6), 데모 레포 2개를 인덱싱해(인덱스 4개) 첫 정확도 측정을 했다.

**쟀다**

- `fastapi-cli` gold 61문항(답 있음 46 · hard negative 15) × 청킹 2종(`--lines` / `--ast`) × 검색 2종(vector / hybrid) × 모드 2종
  run `20260827T061331Z-254b04` · 코퍼스 commit `10d7e65a` · store `pgvector`
- `api-test` 8문항 — run `20260827T061531Z-165f40`

**나왔다** (노이즈선 = 1문항 = 2.2%p)

- **AST 청킹 + 맥락 헤더가 명확히 낫다** — Hit@1 19.6% → 41.3% (+21.7%p, 노이즈선의 10배) · Hit@3 45.7% → 56.5% · MRR 0.362 → 0.519.
  판정 통과 문항도 38/46 → 41/46 으로 늘어, 순위뿐 아니라 "근거 있음" 판정도 함께 좋아진다
- **BM25 하이브리드의 이득은 AST 위에서 사라진다** — `--lines` 에서는 Hit@3 +6.5%p 로 확실하지만, `--ast` 에서는 +2.2%p(1문항, 노이즈)이고 Hit@1 은 오히려 −4.3%p.
  맥락 헤더가 심볼명을 청크 본문에 넣어 BM25 의 역할을 흡수한 것으로 보인다
- **임계값 0.54 가 balanced accuracy 최고점(91.2%)** — 0.50 으로 내리면 정답 +2문항에 hard negative 방어 −3문항, 0.46 이면 +3문항에 −10문항
- `api_test` 는 답 있음 문항이 6개뿐이라 **판정에 쓸 수 없다**(1문항이 16.7%p)

**판단**

- 기본 청커 **`ast-v1` + 맥락 헤더 on 확정** — 실측이 [제안 E] 를 뒷받침한다. 발표에서 개선의 공로는 AST 청킹과 헤더이지 하이브리드가 아니다
- **임계값 0.54 유지** — 바꿀 근거가 없다. 9/1 재보정은 팀원 40문항이 들어온 뒤 재확인으로 축소
- 저장소 **pgvector 확정**. Chroma 기준선 6개는 만들지 않았다 — "저장소를 바꿔도 검색이 안 깨지는가"는 실제 PostgreSQL 왕복 테스트 10/10 이 이미 증명해, 인덱싱을 두 번 돌리는 GPU 약 2시간을 아꼈다
- 코퍼스 경로를 `/srv/repos` → **`~/repos`** 로. 홈 아래여야 sudo·소유권 손질 없이 파일을 올릴 수 있다
- **`POST /v1/chat` 의 `project_id` 는 이제 레포 이름**(`api_test`)이다. 어느 인덱스가 답할지는 서버가 `VSS_PROJECT_ALIASES` 로 정하고 응답의 `index_id` 로 알린다 —
  RAG 를 개선해 인덱스를 갈아타도 Extension 을 고치지 않는다. 계약은 `docs/API.md`

**넘어간 이유**: pgvector 관문 조건 1·2·3 을 충족했다. 이후 정확도 작업은 전부 이 기준선과의 비교로 판정한다.

**아직 아니다**

- `rag_lab` 미배치·미측정 — 데모 시나리오 S3(규약 질의)·S4(브리핑)가 여기 걸려 있다
- `api_test` 는 8문항이라 판정 불가 — 팀원 40문항 gold 대기
- **생성 품질은 재지 않았다.** 오늘 잰 것은 검색까지다. 답변이 근거를 제대로 인용하는지, 한국어가 자연스러운지는 미측정
- hnsw 근사 검색이 정확도를 깎는지 미확인 — `VSS_PG_EXACT=1` 로 질의만 다시 하면 확인된다(재인덱싱 불필요)
- 절대 수준은 아직 낮다. Hit@3 56.5% 는 10문항 중 4문항은 상위 3위 안에서 정답을 못 찾는다는 뜻이다

**자세히**: [RAG_BASELINE_20260827.md](RAG_BASELINE_20260827.md) — RAG 기초 · 용어 · 단계별 경위 · 증명되지 않은 것 7가지
